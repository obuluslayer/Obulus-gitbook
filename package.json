{
  "name": "obulus-docs",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "description": "Obulus Layer documentation — VitePress site (protocol, contracts, Hub API, agent SDK).",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "@fontsource/jetbrains-mono": "^5.3.0",
    "@fontsource/poppins": "^5.3.0",
    "vitepress": "^1.6.4"
  },
  "engines": {
    "node": ">=20"
  }
}
